<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mirrormetech.corm.core.user.domain.repository.RoleRepository">
    <select id="findRolesByUserId" resultType="com.mirrormetech.corm.core.user.infra.Role">
        SELECT * FROM role WHERE user_id = #{userId}
    </select>
</mapper>
